projects:
  - name: foundation
    dir: environments/dev
    workflow: default

  - name: semantic-search
    dir: environments/apps/semantic-search
    workflow: default

    depends_on: ["foundation"]

workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - init
        - apply